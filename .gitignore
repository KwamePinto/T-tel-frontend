# dependencies and build output
node_modules
dist
dist-ssr
*.local

# Vite reads .env for VITE_* values. Those are compiled into the bundle and
# are public, but the file still holds machine-specific URLs — keep it out.
.env
.env.*
!.env.example

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# editors / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sw?
